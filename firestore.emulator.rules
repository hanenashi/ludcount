rules_version = '2';

// Phase 2 development only. Production continues to use firestore.rules.
// Phase 3 will replace this broad authenticated rule with tested membership rules.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
