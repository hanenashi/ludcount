rules_version = '2';

// Development only. Production uses the strict, emulator-tested firestore.rules.
// This broad authenticated rule keeps local UI workflows easy to exercise.
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if request.auth != null;
    }
  }
}
